
# Requirement
Any Browsers
# Installing
Our fingerprinting script does not require any installation
# Config
Copy the `config.template.js to config.js` are add the configs you get from firebase
# Run
You just need to open `index.html` using a browser
# Note
This study uses `hybridOscialltor.js` as a fingerprinting script